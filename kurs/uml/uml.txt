@startuml
skinparam monochrome true
hide circle
hide methods

left to right direction
actor P1 as "Администратор"
actor P2 as "Оператор"
actor M1 as "ИС"

  usecase F1 as "Ввод оборудования в систему управления контроллеров"
  usecase F2 as "Заполнение API токенов"
  usecase F3 as "Заполнение профилей печати 3d принтеров"
  usecase F4 as "Импортирование 3d моделей изделий"
  usecase F5 as "Формирование производственной задачи"


  P1 -- (F1)
  P1 -- (F2)
  P2 -- (F3)
  P2 -- (F4)
  P2 -- (F5)

  (F1) -- M1
  (F2) -- M1
  (F3) -- M1
  (F4) -- M1
  (F5) -- M1
@enduml